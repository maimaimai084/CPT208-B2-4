# AI Coding Logs

The coursework PDF specifies the folder name `/ai-logs`. The full AI coding log has therefore been placed in:

`ai-logs/README.md`

This folder is kept only as a duplicate-friendly note because an earlier version used the name `ai logs`.
